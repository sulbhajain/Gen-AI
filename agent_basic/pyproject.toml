[project]
name = "agent-basic"
version = "0.1.0"
description = "Customer service agent using Ollama and FAISS"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
  "langchain>=0.2.0",
  "langchain-community>=0.2.0",
  "langchain-ollama>=0.1.0",
  "faiss-cpu>=1.8.0",
  "streamlit>=1.32.0",
  "watchdog>=4.0.0",
  "requests>=2.31.0",
  "python-dotenv>=1.0.1",
]

[tool.uv]
# uv will generate uv.lock on install
